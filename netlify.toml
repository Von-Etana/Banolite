[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # Tell Netlify that these keys are INTENDED to be public and safe to bundle
  SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_SUPABASE_URL,NEXT_PUBLIC_SUPABASE_ANON_KEY,NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY,RESEND_FROM_EMAIL"
